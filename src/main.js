$(document).ready(function () {
    $(".date-field1").pDatepicker({
        format: 'YYYY/MM/DD',
        autoClose: true,
    });

    $(".date-field2").pDatepicker({
        format: 'YYYY/MM/DD',
        autoClose: true,
    });
});
